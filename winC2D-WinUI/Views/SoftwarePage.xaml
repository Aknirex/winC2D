<Page
    x:Class="winC2D_WinUI.Views.SoftwarePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:winC2D_WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <Button x:Name="BtnRefresh" Click="BtnRefresh_Click" Icon="Refresh" Content="Refresh" />
            <Button x:Name="BtnMigrate" Click="BtnMigrate_Click" Icon="Move" Content="Migrate Selected" Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>

        <ListView x:Name="ListSoftware" Grid.Row="1" SelectionMode="Extended">
            <ListView.Header>
                <Grid Padding="12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="3*" />
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Name" FontWeight="Bold"/>
                    <TextBlock Grid.Column="1" Text="Location" FontWeight="Bold"/>
                    <TextBlock Grid.Column="2" Text="Size" FontWeight="Bold"/>
                    <TextBlock Grid.Column="3" Text="Status" FontWeight="Bold"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*" />
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Grid.Column="1" Text="{Binding InstallLocation}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Opacity="0.7"/>
                        <TextBlock Grid.Column="2" Text="{Binding SizeText}" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,16,0"/>
                        <TextBlock Grid.Column="3" Text="{Binding Status}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,8,0,0" Opacity="0.6"/>
    </Grid>
</Page>
