<UserControl x:Class="winC2D.App.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Margin="10">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <ui:SymbolIcon Symbol="History24" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <TextBlock Text="{Binding L_Header}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            <ui:Button Command="{Binding LoadLogsCommand}" 
                       Appearance="Primary"
                       Margin="20,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="14"/>
                    <TextBlock Text="{Binding L_Refresh}" Margin="5,0,0,0"/>
                </StackPanel>
            </ui:Button>
        </StackPanel>
        
        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <CheckBox Content="{Binding L_ShowRunning}" IsChecked="{Binding ShowRunning}" Margin="0,0,15,0"/>
            <CheckBox Content="{Binding L_ShowSuccessful}" IsChecked="{Binding ShowSuccessful}" Margin="0,0,15,0"/>
            <CheckBox Content="{Binding L_ShowFailed}" IsChecked="{Binding ShowFailed}" Margin="0,0,15,0"/>
            <CheckBox Content="{Binding L_ShowRolledBack}" IsChecked="{Binding ShowRolledBack}"/>
        </StackPanel>
        
        <!-- Log List -->
        <DataGrid Grid.Row="2"
                    ItemsSource="{Binding EntriesView}"
                    SelectedItem="{Binding SelectedEntry, Mode=TwoWay}"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    SelectionMode="Single"
                    SelectionUnit="FullRow"
                    HeadersVisibility="Column"
                    GridLinesVisibility="Horizontal"
                    RowHeight="32"
                    ColumnHeaderHeight="32"
                    Background="{DynamicResource CardBackgroundBrush}">
            <DataGrid.Resources>
                <Style x:Key="LogCellTextStyle" TargetType="TextBlock">
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                </Style>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource CardStrokeColorDefaultBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentFillColorSecondaryBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Time, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColTime, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Name}" Width="140" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColName, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Status}" Width="110" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Progress}" Width="80" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColProgress, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Files}" Width="90" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColFiles, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding SourcePath}" Width="1.4*" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColSource, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding TargetPath}" Width="1.4*" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColTarget, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Message}" Width="2*" ElementStyle="{StaticResource LogCellTextStyle}">
                    <DataGridTextColumn.Header>
                        <TextBlock Text="{Binding DataContext.L_ColMessage, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataGridTextColumn.Header>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Actions -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:Button Command="{Binding RollbackCommand}"
                          Content="{Binding L_RollbackSelected}"
                          Margin="0,0,10,0"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Command="{Binding ExportLogCommand}" Content="{Binding L_ExportLog}" Margin="0,0,10,0"/>
                <ui:Button Command="{Binding ClearStaleCommand}" Content="{Binding L_ClearStale}" Margin="0,0,10,0"/>
                <ui:Button Command="{Binding ClearLogCommand}" Content="{Binding L_ClearHistory}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
