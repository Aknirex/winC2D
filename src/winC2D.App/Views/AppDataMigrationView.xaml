<UserControl x:Class="winC2D.App.Views.AppDataMigrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Margin="10">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="📁" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="AppData Migration" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <ui:Button Command="{Binding ScanCommand}" 
                       Appearance="Primary"
                       Margin="20,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Refresh" FontSize="14"/>
                    <TextBlock Text="Refresh" Margin="5,0,0,0"/>
                </StackPanel>
            </ui:Button>
        </StackPanel>
        
        <!-- Search -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <ui:TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                       PlaceholderText="Search AppData folders..."
                       Icon="{ui:SymbolIcon Search}"/>
        </Grid>
        
        <!-- AppData List -->
        <ui:DataGrid Grid.Row="2"
                    ItemsSource="{Binding AppDataItems}"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, Mode=TwoWay}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="300"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </ui:DataGrid>
        
        <!-- Target and Actions -->
        <Grid Grid.Row="3" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                <TextBlock Text="Target: " VerticalAlignment="Center"/>
                <ui:TextBox Width="250" Text="{Binding TargetPath}" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ui:Button Command="{Binding MigrateCommand}"
                          Appearance="Primary"
                          IsEnabled="{Binding IsMigrating, Converter={StaticResource InverseBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowRight" FontSize="14"/>
                        <TextBlock Text="Migrate Selected" Margin="5,0,0,0"/>
                    </StackPanel>
                </ui:Button>
            </StackPanel>
        </Grid>
        
        <!-- Status -->
        <StatusBar Grid.Row="4">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>
    </Grid>
</UserControl>