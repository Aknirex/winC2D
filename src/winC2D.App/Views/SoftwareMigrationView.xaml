<UserControl x:Class="winC2D.App.Views.SoftwareMigrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:vm="clr-namespace:winC2D.App.ViewModels"
             Margin="10">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="📦" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Software Migration" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            <ui:Button Command="{Binding ScanCommand}" 
                       Appearance="Primary"
                       Margin="20,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Refresh" FontSize="14"/>
                    <TextBlock Text="Refresh" Margin="5,0,0,0"/>
                </StackPanel>
            </ui:Button>
        </StackPanel>
        
        <!-- Search -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ui:TextBox Grid.Column="0" 
                       Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                       PlaceholderText="Search software..."
                       Icon="{ui:SymbolIcon Search}"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                <ui:Button Command="{Binding SelectAllCommand}" Content="Select All" Margin="0,0,5,0"/>
                <ui:Button Command="{Binding DeselectAllCommand}" Content="Deselect All"/>
            </StackPanel>
        </Grid>
        
        <!-- Software List -->
        <ui:DataGrid Grid.Row="2"
                    ItemsSource="{Binding SoftwareItems}"
                    SelectedItems="{Binding SelectedItems, Mode=TwoWay}"
                    AutoGenerateColumns="False"
                    IsReadOnly="True"
                    SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, Mode=TwoWay}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Path" Binding="{Binding InstallLocation}" Width="300"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeText}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </ui:DataGrid>
        
        <!-- Selection Info -->
        <Grid Grid.Row="3" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Selected: " FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalSelectedCount}"/>
                <TextBlock Text=" items (" Margin="5,0,0,0"/>
                <TextBlock Text="{Binding TotalSelectedSize}"/>
                <TextBlock Text=" bytes)"/>
            </StackPanel>
        </Grid>
        
        <!-- Target and Actions -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,20,0">
                <TextBlock Text="Target Disk: " VerticalAlignment="Center"/>
                <ComboBox Width="80" 
                         SelectedItem="{Binding TargetDisk}"
                         ItemsSource="{Binding AvailableDrives}"/>
                <TextBlock Text=" Path: " VerticalAlignment="Center" Margin="10,0,0,0"/>
                <ui:TextBox Width="200" Text="{Binding TargetPath}" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <ui:Button Command="{Binding MigrateCommand}"
                          Appearance="Primary"
                          IsEnabled="{Binding IsMigrating, Converter={StaticResource InverseBooleanConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="ArrowRight" FontSize="14"/>
                        <TextBlock Text="Migrate Selected" Margin="5,0,0,0"/>
                    </StackPanel>
                </ui:Button>
            </StackPanel>
        </Grid>
        
        <!-- Progress Bar (shown during migration) -->
        <Grid Grid.Row="5" Visibility="{Binding IsMigrating, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Margin="0,10,0,0">
                <TextBlock Text="{Binding StatusMessage}"/>
                <ProgressBar Value="{Binding MigrationProgress}" Maximum="100" Height="20" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>